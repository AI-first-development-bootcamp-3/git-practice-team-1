## 1. Status Chart Component
- [x] 1.1 Create a reusable, dependency-free horizontal status chart component.
- [x] 1.2 Calculate proportional bar widths from the existing `tasksByStatus` DTO.
- [x] 1.3 Display accessible status labels and exact counts without relying on color alone.
- [x] 1.4 Add an explicit empty state when all status counts are zero.

## 2. Statistics Integration and Styling
- [x] 2.1 Integrate the status chart into the existing Statistics view without additional API requests.
- [x] 2.2 Add responsive chart styles consistent with the existing status colors and card design.

## 3. Validation
- [x] 3.1 Verify populated and all-zero chart states.
- [x] 3.2 Verify existing Statistics loading and error states remain unchanged.
- [x] 3.3 Run the client production build and strict OpenSpec validation.
